[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  # Exclude Next.js public environment variables from secrets scanning
  # These are intentionally exposed to the browser
  SECRETS_SCAN_OMIT_KEYS = "NEXT_PUBLIC_BASE_URL"

[[plugins]]
  package = "@netlify/plugin-nextjs"
